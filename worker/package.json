{
  "name": "server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "app.js",
  "scripts": {
    "build": "npm run build",
    "dev": "nodemon app.js",
    "start": "node app.js "
  },
  "dependencies": {
    "express": "^4.18.2",
    "amqplib": "^0.10.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "nodemonConfig": {
    "ignore": [
      "storage/*"
    ]
  }
}